const auth = require('./auth');
const donation = require('./donation');
const donationNews = require('./donationnews');
const donationTransaction = require('./donationtransaction');

module.exports = {
    auth,
    donation,
    donationNews,
    donationTransaction,
};